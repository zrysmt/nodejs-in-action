#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ws@0.4.32@ws/bin/wscat" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ws@0.4.32@ws/bin/wscat" "$@"
  ret=$?
fi
exit $ret
